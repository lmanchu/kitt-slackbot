# Kitt 知識庫自動更新系統

> 實作日期：2025-11-13
> 版本：v2.0

---

## 🎯 功能說明

Kitt 現在能夠**即時同步** IrisGo PM 知識庫，當你在 Obsidian 或任何編輯器中更新 PM 文件時，Kitt 會自動感知變化並重新載入，無需手動重啟。

---

## 📁 監控的知識庫檔案

路徑：`~/Dropbox/PKM-Vault/1-Projects/IrisGo/Product/`

監控檔案：
- ✅ `knowledge-base.md` - 產品核心資訊
- ✅ `customers.md` - 客戶與合作夥伴資訊
- ✅ `roadmap.md` - 產品路線圖
- ✅ `priorities.md` - 當前優先事項
- ✅ `resources.md` - 參考資源

---

## 🚀 工作原理

### 1. 啟動載入
Kitt 啟動時自動載入所有知識庫檔案到記憶體：

```
📚 Loading IrisGo knowledge base...
  ✓ Loaded knowledge-base.md (10093 chars)
  ✓ Loaded customers.md (1484 chars)
  ✓ Loaded roadmap.md (2974 chars)
  ✓ Loaded priorities.md (7192 chars)
  ✓ Loaded resources.md (2423 chars)
✓ Knowledge base loaded at 2025-11-13T13:09:54.089Z
```

### 2. 即時監控
使用 Node.js `fs.watch()` 監控目錄變化：

```
👁️  Watching /Users/lman/Dropbox/PKM-Vault/1-Projects/IrisGo/Product for changes
```

### 3. 自動更新
當你修改任何 `.md` 檔案時：

```
🔄 Detected change in knowledge-base.md, reloading knowledge base...
📚 Loading IrisGo knowledge base...
  ✓ Loaded knowledge-base.md (10094 chars)
  ...
✓ Knowledge base loaded at 2025-11-13T13:10:16.365Z
```

**更新延遲**：< 1 秒

---

## 💡 使用範例

### 場景一：更新 GoT Token 資訊

1. 你在 Obsidian 中打開 `knowledge-base.md`
2. 添加新的 GoT Token 經濟模型說明
3. 儲存檔案
4. **Kitt 自動重新載入** (無需任何操作)
5. 下次團隊成員在 Slack 問「GoT Token 是什麼？」
6. Kitt 回答時會包含最新的資訊

### 場景二：更新優先事項

1. 你在 `priorities.md` 中調整本週的優先級
2. 儲存檔案
3. **Kitt 立即感知變化**
4. 團隊成員問「這週我們的重點是什麼？」
5. Kitt 提供最新的優先事項清單

---

## 🔍 監控與驗證

### 查看載入狀態

檢查 Kitt 日誌中的知識庫載入時間：

```bash
tail -100 ~/kitt-slackbot/kitt.log | grep "Knowledge base loaded"
```

輸出範例：
```
✓ Knowledge base loaded at 2025-11-13T13:10:16.365Z
```

### 測試自動更新

修改任一 PM 檔案並檢查日誌：

```bash
# 1. 修改檔案（例如添加一行註解）
echo "<!-- Test update -->" >> ~/Dropbox/PKM-Vault/1-Projects/IrisGo/Product/knowledge-base.md

# 2. 查看 Kitt 日誌（應該立即看到重新載入訊息）
tail -20 ~/kitt-slackbot/kitt.log | grep "🔄\|Detected"
```

預期輸出：
```
🔄 Detected change in knowledge-base.md, reloading knowledge base...
```

---

## 📊 技術細節

### 記憶體使用

- 5 個 MD 檔案，總計約 24KB
- 全部載入記憶體後：< 30KB
- 對 Node.js 應用影響：可忽略

### 效能影響

- **啟動時間增加**：+50ms（讀取 5 個檔案）
- **回應延遲**：無影響（知識庫已在記憶體中）
- **檔案監控開銷**：< 0.1% CPU

### 智能載入

Kitt 在生成回應時，會根據問題性質選擇性載入知識：

```javascript
// 產品資訊：前 3000 字元
const productInfo = knowledgeBase.product.substring(0, 3000);

// 當前優先事項：前 1000 字元
const currentPriorities = knowledgeBase.priorities.substring(0, 1000);
```

這樣可以避免傳送過多內容給 Ollama，保持回應速度。

---

## 🔄 與 AIPM 的協作

| 系統 | 角色 | 知識庫來源 | 更新方式 |
|------|------|-----------|---------|
| **Kitt** | Slack 團隊助手 | 記憶體中的 MD 檔案 | 即時檔案監控 |
| **AIPM** | PM 角色專家 | 讀取 MD 檔案 | `/pm` 指令觸發 |

兩者共用同一套知識庫檔案：`~/Dropbox/PKM-Vault/1-Projects/IrisGo/Product/`

---

## ⚠️ 注意事項

### 1. Dropbox 同步延遲

如果你在另一台機器（如 MacBook Air）上修改 PM 檔案：
- Dropbox 需要時間同步到 Mac Studio
- Kitt 會在同步完成後自動載入
- 通常延遲：< 5 秒

### 2. 大型檔案

目前系統會載入整個檔案到記憶體：
- 建議單個 MD 檔案 < 100KB
- 如果檔案過大，考慮拆分成多個檔案

### 3. 檔案權限

確保 Kitt 有讀取權限：
```bash
ls -la ~/Dropbox/PKM-Vault/1-Projects/IrisGo/Product/
```

---

## 🚀 未來擴展

### Phase 2: RAG (Retrieval-Augmented Generation)

當知識庫成長到 20+ 份文件時，考慮升級到向量資料庫：

1. 設置 ChromaDB 或 Qdrant
2. 將 MD 檔案嵌入（embedding）
3. 根據問題檢索相關片段
4. 只傳送相關內容給 Ollama

**優勢**：
- 支援數百份文件
- 更精準的知識檢索
- 更低的 token 消耗

---

## 📝 Changelog

### v2.0 (2025-11-13)
- ✅ 新增知識庫自動載入系統
- ✅ 新增檔案監控（`fs.watch`）
- ✅ 更新 `generateAIResponse` 使用動態知識庫
- ✅ 知識庫載入時間戳記錄
- ✅ 智能內容截取（避免 token 浪費）

### v1.0 (2025-11-09)
- 基礎 Kitt Slack Bot 功能
- 硬編碼的 IrisGo 資訊

---

**維護者**: Iris (Melchior)
**最後更新**: 2025-11-13
